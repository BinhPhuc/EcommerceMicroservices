spring:
  datasource:
    url: jdbc:mysql://localhost:3306/orders
    username: root
    password: rootpassword

  jpa:
    hibernate:
      ddl-auto: update

services:
  product:
    base-url: http://localhost:8081
    timeout: 5s
