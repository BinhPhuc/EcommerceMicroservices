server:
  port: 8089

spring:
  application:
    name: favourite-service

  datasource:
    url: jdbc:mysql://localhost:3306/favourites
    username: root
    password: rootpassword

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
