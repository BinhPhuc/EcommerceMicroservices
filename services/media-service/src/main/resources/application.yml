server:
  port: 8088

spring:
  application:
    name: media-service

  datasource:
    url: jdbc:mysql://localhost:3306/medias
    username: root
    password: rootpassword

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
